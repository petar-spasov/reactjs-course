import React from 'react';

const user = (props) => (
    <div>
        <h1>{props.name}</h1>
        <p>{props.agex}</p>
        <style jsx>
            {
                `
                
              `
            }
        </style>
    </div>
);